{
  "name": "amplify-test",
  "type": "module",
  "scripts": {
    "sandbox": "amplify sandbox start --outputs-format ts --outputs-version 1.2"
  },
  "dependencies": {
    "@aws-amplify/backend": "^0.6.0",
    "@aws-amplify/cli": "^12.10.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
